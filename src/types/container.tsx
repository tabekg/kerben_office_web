import { Color } from "react-bootstrap/esm/types";

export interface ContainerItems {
    title: number;
    label: string;
    number: number;
    city1: string;
    city2: string;
    lightHouseColor: Color;
  }